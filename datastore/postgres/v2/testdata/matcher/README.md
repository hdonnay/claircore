The `.jsonseq` files here are `application/json-seq` ([RFC7464](https://www.rfc-editor.org/rfc/rfc7464.html)) formatted.
When editing, make sure to pass the files through `jq --seq` to ensure the hard-to-type ␞ character is in-place.
